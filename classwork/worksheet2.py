def Task1():
    pass
    
    """
    1)
    False
    True
    True
    False
    True
    False
    """


    # Written in pseudocode
    """
    2)
    age = input("Enter age")
    if age > 10 AND age < 19 then
        print("Valid pupil age")
    else
        print("Invalid input: enter a value from 11 to 18")
    endif
    """

def Task2():
    pass

    # Written in pseudocode
    """
    3.a)
    value = input("Enter the value of your order")
    nextday = input("Do you want next day delivery?")
    total = 0
    if nextday == "yes" then
        total = total + 5
    endif
    if value < 15 then
        total = total + 3.50
    endif
    """

    """
    3.b)
    (i) 3.50
    (ii) 0
    (iii) 5
    """

    # Written in pseudocode
    """
    4)
    attendance = input("Input attendance percentage")
    mark = input("Enter mark")

    if attendance <= 90 OR mark <= 70 then
        print("fail")
    else
        if mark > 90 then
            print("Grade A")
        elseif mark > 80 then
            print("Grade B")
        elseif mark > 70 then
            print("Grade C")
        endif
    endif
    """

def Task3():
    pass
    # Written in pseudocode
    """
    5.a)
    while trigger == "On"
        if movementGround then
            SEND "intruder on ground floor"
            Alarm = ON
        endif
        if movementFirst then
            Send "intruder on first floor"
            Alarm = On
        endif
    endwhile
    """

    """
    5.b)
    while trigger == "On"
        if movementGround == True then
            
    """